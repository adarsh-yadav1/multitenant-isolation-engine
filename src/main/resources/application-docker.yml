# Applied when SPRING_PROFILES_ACTIVE=docker (set in docker-compose.yml)
spring:
  jpa:
    show-sql: false
  lifecycle:
    timeout-per-shutdown-phase: 30s

logging:
  level:
    root: INFO
    com.saas.multitenant: INFO

server:
  shutdown: graceful